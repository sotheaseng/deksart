-- Create the hotel_blockchain database
CREATE DATABASE IF NOT EXISTS hotel_blockchain;
USE hotel_blockchain;

-- Grant privileges (adjust as needed for your setup)
GRANT ALL PRIVILEGES ON hotel_blockchain.* TO 'root'@'localhost';
FLUSH PRIVILEGES;
